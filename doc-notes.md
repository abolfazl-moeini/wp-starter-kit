## JS - TODO

- [x] build:assets + --validate / --dry-run are implemented (see build/build-assets.js + package.json scripts + tests/build/\*).
- [x] assetMappings documented in docs/asset-mappings.md and docs/build-system.md.
- [ ] document how to use ROOT_NAME env variable example (non-blocking; the primary path is project.config.json + seeded value).
- [x] All core build commands wired and exercised by tests + smoke runs.
