# menu-builder

Admin menu registration.

## Dependencies

core

## Usage

```php
add_action( 'wpdev_menu-builder_register', function () {
	// Register components
} );
```

## Legacy bridge

Maps to `WPDevFramework\Admin_Pages\Base_Admin_Page` via `class_alias` until full migration.
