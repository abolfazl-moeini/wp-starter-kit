# form-builder

Form render pipeline and modal forms.

## Dependencies

core, field-builder

## Usage

```php
add_action( 'wpdev_form-builder_register', function () {
	// Register components
} );
```

## Legacy bridge

Maps to `WPDevFramework\UI\Form` via `class_alias` until full migration.
