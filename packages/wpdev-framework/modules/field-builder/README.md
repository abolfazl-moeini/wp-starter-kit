# field-builder

Admin field types and sanitize contract.

## Dependencies

core

## Usage

```php
add_action( 'wpdev_field-builder_register', function () {
	// Register components
} );
```

## Legacy bridge

Maps to `WPDevFramework\UI\Field` via `class_alias` until full migration.
