# AI skills feed notes

Extract agent skills from colocated `API_DOC.md` files plus this index layer.

## Recommended skill topics

1. **Module bootstrap** — `wpdev_load_module`, lifecycle hooks, `setup.php` pattern
2. **Admin pages** — extend `List_Admin_Page` / `Edit_Admin_Page`, register with `wpdev_register_module_admin_pages`
3. **Forms and fields** — `wpdev_register_form`, `wpdev_register_settings_field`, field contexts
4. **Tables** — `wpdev_register_table`, ajax refresh, screen options
5. **Settings** — `Settings_Storage`, section registry, save handlers
6. **Playground** — `wpdev_register_playground_panel`, production parity registry

## Symbol coverage

- Total tracked symbols: 810
- Manifest: [`manifest.json`](manifest.json)
