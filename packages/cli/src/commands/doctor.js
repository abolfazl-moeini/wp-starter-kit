/**
 * `wpsk doctor` — diagnose system prerequisites and project drift.
 *
 * Phase I1: stub. Phase I5 wires the system-tool checks and
 * `engine.doctorProject(dir)`.
 *
 * @param {object} opts
 * @param {boolean} [opts.verbose]
 */
export async function runDoctor(_opts) {
  throw new Error("wpsk doctor: not implemented (Phase I5)");
}
