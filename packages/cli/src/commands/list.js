/**
 * `wpsk list` — print the features in the current project's
 * `wpsk-kit.json` (manifest).
 *
 * Phase I1: stub. Phase I4 reads the manifest via the engine.
 *
 * @param {object} opts
 * @param {boolean} [opts.verbose]
 */
export async function runList(_opts) {
  throw new Error("wpsk list: not implemented (Phase I4)");
}
