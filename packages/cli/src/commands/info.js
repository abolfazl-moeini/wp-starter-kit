/**
 * `wpsk info` — print kit version, feature states, and available
 * updates for the current project.
 *
 * Phase I1: stub. Phase I5 wires `engine.getKitStatus(dir)`.
 *
 * @param {object} opts
 * @param {boolean} [opts.verbose]
 */
export async function runInfo(_opts) {
  throw new Error("wpsk info: not implemented (Phase I5)");
}
