/**
 * `wpsk remove <feature>` — remove a feature from an existing project.
 *
 * Phase I1: stub. Phase I4 wires `engine.removeFeature(dir, id)`.
 *
 * @param {object} opts
 * @param {string} opts.feature  required feature id
 * @param {boolean} [opts.yes]
 * @param {boolean} [opts.verbose]
 */
export async function runRemove(_opts) {
  throw new Error("wpsk remove: not implemented (Phase I4)");
}
