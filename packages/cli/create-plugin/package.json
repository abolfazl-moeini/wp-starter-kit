{
  "name": "@wpsk/create-plugin",
  "version": "0.1.0",
  "description": "Tiny shim for `npm create @wpsk/plugin` (plan.installer.md Phase I7 — I7.4). Forwards argv to the @wpsk/cli `create` subcommand. The real scaffolder lives in @wpsk/create-wp-project.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "bin": {
    "create-wpsk-plugin": "bin/create-wpsk-plugin.js"
  },
  "files": [
    "bin"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@wpsk/cli": "^0.1.0"
  }
}
